meta:
  environment: python
  toolVersion: "3.11"
build:
  installDependencies: true
  buildDirectory: .
run:
  persistenceMount: /data
  containerPort: 8000
  command: uvicorn main:app --host 0.0.0.0 --port 8000
